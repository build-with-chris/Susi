export const metadata = {
  title: "Neues Projekt",
  description: "Neues Projekt anlegen und Videos hochladen",
};

export default function ProjektNeuLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
